{# -*- mode: jinja2; -*- #}
{%- macro heading(text, title) -%}
{% if cookiecutter.__use_markdown == "y" -%}
{{ "#" if title else "##" }} {{ text }}
{%- else -%}
{{ text }}
{% for _ in text %}{{ "=" if title else "-" }}{% endfor %}
{%- endif %}
{%- endmacro -%}
{{ heading("Usage", True) }}


{% if cookiecutter.__use_markdown == "True" -%}
```eval_rst
{%- endif %}
.. click:: {{ cookiecutter.__package_name }}.__main__:{{ cookiecutter.__package_name }}
   :prog: {{ cookiecutter.__project_slug }}
   :nested: full
{% if cookiecutter.__use_markdown == "y" -%}
```
{%- endif -%}
