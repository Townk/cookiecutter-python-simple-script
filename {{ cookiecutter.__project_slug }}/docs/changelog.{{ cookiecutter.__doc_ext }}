{%- macro heading(text, title) -%}
{% if cookiecutter.__use_markdown == "y" -%}
{{ "#" if title else "##" }} {{ text }}
{%- else -%}
{{ text }}
{% for _ in text %}{{ "=" if title else "-" }}{% endfor %}
{%- endif %}
{%- endmacro -%}
{%- macro link(text, url) -%}
{% if cookiecutter.__use_markdown == "y" -%}
[{{ text }}]({{ url }})
{%- else -%}
`{{ text }} <{{ url }}>`_
{%- endif %}
{%- endmacro -%}
{{ heading("Changelog", True) }}

{% if cookiecutter.publish_to_github.lower() == "y" -%}
A detailed change log associated with each release is located at
{{ link("this project's release page", "https://github.com/" + cookiecutter.github_user + "/" + cookiecutter.__project_slug + "/releases") }}
on GitHub.

{% endif -%}
{% if cookiecutter.__use_markdown == "True" -%}
```eval_rst
{%- endif %}
.. include:: ../CHANGELOG.{{ cookiecutter.__doc_ext }}
{% if cookiecutter.__use_markdown == "y" -%}
```
{%- endif -%}
